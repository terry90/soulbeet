### Serving The App

Navigate to the platform crate of your choice:

```bash
cd web
```

and serve:

```bash
dx serve --port 9797
```

or directly:

```
dx serve --package web --platform web --port 9797
```
